[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cdr-pedido"
version = "0.7.1"
description = "Interpretação auditável de pedidos de gráfica em arquivos CorelDRAW CDR"
readme = "README.md"
requires-python = ">=3.10"
authors = [{ name = "pedrsilva3515" }]
dependencies = []

[project.optional-dependencies]
gui = ["tkinterdnd2>=0.6.3", "pywin32>=306; sys_platform == 'win32'", "rapidocr-onnxruntime==1.4.4"]
api = ["openai>=2.0", "keyring>=25"]

[project.scripts]
cdr-pedido = "zcfreader.cli:main"
cdr-pedido-gui = "zcfreader.gui:main"

[tool.setuptools]
package-dir = { "" = "fase3-parser" }
packages = ["zcfreader"]
